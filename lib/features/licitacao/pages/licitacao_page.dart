import 'package:flutter/material.dart';

class LicitacaoPage extends StatelessWidget {
  const LicitacaoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Módulo 2 — Licitação (Fase 5)'),
    );
  }
}
