import 'package:flutter/material.dart';

class AtaPage extends StatelessWidget {
  const AtaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Módulo 3 — Ata (Fase 6)'),
    );
  }
}
