import 'package:flutter/material.dart';

class EditalPage extends StatelessWidget {
  const EditalPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Módulo 1 — Edital (Fase 4)'),
    );
  }
}
