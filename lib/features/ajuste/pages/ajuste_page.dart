import 'package:flutter/material.dart';

class AjustePage extends StatelessWidget {
  const AjustePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Módulo 4 — Ajuste (Fase 7)'),
    );
  }
}
